# Ensure container auth directories exist at boot.
# Credentials are injected at runtime via `podman login`, not baked into the image.

# Root podman auth directory
d /run/containers/0 0755 root root -

# Ostree/bootc auth directory
d /etc/ostree 0755 root root -
