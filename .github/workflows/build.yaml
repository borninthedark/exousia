name: Bootc DevSecOps Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      fedora_version:
        description: 'Fedora version to build'
        required: true
        type: choice
        options:
          - 'current'
          - '42'
          - '43'
          - '41'
          - 'rawhide'
        default: 'current'
      image_type:
        description: 'Bootc base image type'
        required: true
        type: choice
        options:
          - 'current'
          - 'fedora-bootc'
          - 'fedora-sway-atomic'
        default: 'current'
      enable_plymouth:
        description: 'Enable Plymouth boot splash'
        required: true
        type: boolean
        default: true
  schedule:
    - cron: "20 4 * * *" # Nightly build at 4:20 AM UTC

permissions:
  contents: write
  packages: write
  id-token: write
  pull-requests: write
  security-events: write

concurrency:
  group: bootc-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/${{ github.repository_owner }}/exousia
  DOCKERHUB_IMAGE_BASE: docker.io/1borninthedark/exousia

jobs:
  # ══════════════════════════════════════════════════════════════════════════
  # Job 1: Lint & Validate - Static Analysis and File Validation
  # ══════════════════════════════════════════════════════════════════════════
  lint-and-validate:
    name: Lint & Validate
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify required file structure
        run: |
          echo "::group::File Structure Validation"
          echo "Verifying required configuration files..."

          if [[ ! -d custom-configs/sway ]]; then
            echo "::error::Missing custom-configs/sway directory"
            exit 1
          fi

          for file in sway.desktop environment start-sway; do
            if [[ ! -f "custom-configs/sway/$file" ]]; then
              echo "::error::Missing custom-configs/sway/$file"
              exit 1
            fi
          done
          echo "✓ All required Sway configuration files present"

          for file in packages.add packages.remove packages.sway; do
            if [[ ! -f "custom-pkgs/$file" ]]; then
              echo "::error::Missing custom-pkgs/$file"
              exit 1
            fi
          done
          echo "✓ All package list files present"

          for script in setup-plymouth-theme dracut-rebuild fedora-version-switcher; do
            if [[ ! -f "custom-scripts/$script" ]]; then
              echo "::error::Missing custom-scripts/$script"
              exit 1
            fi
          done
          echo "✓ All required scripts present"
          echo "::endgroup::"

      - name: Lint Containerfile.atomic
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Containerfile.atomic
          ignore: DL3041,DL3059,SC2086,DL4006,SC3037

      - name: Lint Containerfile.bootc
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Containerfile.bootc
          ignore: DL3041,DL3059,SC2086,DL4006,SC3037

      - name: ShellCheck all scripts
        uses: ludeeus/action-shellcheck@master
        with:
          additional_files: 'custom-scripts/*'

      - name: Semgrep security scan
        continue-on-error: true
        uses: returntocorp/semgrep-action@v1
        with:
          config: auto

      - name: Lint summary
        if: always()
        run: |
          {
            echo "## ✅ Lint & Validation Results"
            echo ""
            echo "- Containerfile linting: Completed (hadolint)"
            echo "- Shell script validation: Completed (shellcheck)"
            echo "- Static analysis: Completed (semgrep)"
            echo "- File structure: Validated"
          } >> "$GITHUB_STEP_SUMMARY"

  # ══════════════════════════════════════════════════════════════════════════
  # Job 2: Build - Build Bootc Container Image
  # ══════════════════════════════════════════════════════════════════════════
  build:
    name: Build Bootc Image
    needs: lint-and-validate
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    outputs:
      image_tags: ${{ steps.meta.outputs.tags }}
      first_tag: ${{ steps.first-tag.outputs.tag }}
      build_version: ${{ steps.config.outputs.BUILD_VERSION }}
      build_type: ${{ steps.config.outputs.BUILD_IMAGE_TYPE }}
      containerfile: ${{ steps.config.outputs.CONTAINERFILE }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Detect and resolve Fedora configuration
        id: config
        env:
          INPUT_VERSION: ${{ github.event.inputs.fedora_version || 'current' }}
          INPUT_IMAGE_TYPE: ${{ github.event.inputs.image_type || 'current' }}
          INPUT_ENABLE_PLYMOUTH: ${{ github.event.inputs.enable_plymouth || 'true' }}
        run: |
          echo "::group::Configuration Detection"
          echo "Input version: $INPUT_VERSION"
          echo "Input image type: $INPUT_IMAGE_TYPE"
          echo "Input Plymouth: $INPUT_ENABLE_PLYMOUTH"

          if [[ -f .fedora-version ]] && [[ -s .fedora-version ]]; then
            CURRENT_VERSION=$(cut -d: -f1 .fedora-version)
            CURRENT_IMAGE_TYPE=$(cut -d: -f2 .fedora-version)
            echo "Detected from .fedora-version: $CURRENT_VERSION:$CURRENT_IMAGE_TYPE"
          else
            CURRENT_VERSION="43"
            CURRENT_IMAGE_TYPE="fedora-sway-atomic"
            echo "Using defaults: $CURRENT_VERSION:$CURRENT_IMAGE_TYPE"
          fi

          TARGET_VERSION="${CURRENT_VERSION}"
          TARGET_IMAGE_TYPE="${CURRENT_IMAGE_TYPE}"

          if [[ "$INPUT_VERSION" != "current" ]]; then
            TARGET_VERSION="$INPUT_VERSION"
          fi

          if [[ "$INPUT_IMAGE_TYPE" != "current" ]]; then
            TARGET_IMAGE_TYPE="$INPUT_IMAGE_TYPE"
          fi

          ENABLE_PLYMOUTH="$INPUT_ENABLE_PLYMOUTH"

          echo "Resolved target: Fedora $TARGET_VERSION with $TARGET_IMAGE_TYPE"
          echo "Plymouth enabled: $ENABLE_PLYMOUTH"

          if [[ "$TARGET_IMAGE_TYPE" == "fedora-bootc" ]]; then
            CONTAINERFILE_PATH="./Containerfile.bootc"
          else
            CONTAINERFILE_PATH="./Containerfile.atomic"
          fi

          echo "Using Containerfile: $CONTAINERFILE_PATH"

          if [[ "$TARGET_VERSION" != "$CURRENT_VERSION" ]] || [[ "$TARGET_IMAGE_TYPE" != "$CURRENT_IMAGE_TYPE" ]]; then
            chmod +x ./custom-scripts/fedora-version-switcher
            ./custom-scripts/fedora-version-switcher "$TARGET_VERSION" "$TARGET_IMAGE_TYPE"
            echo "Configuration updated to: $TARGET_VERSION:$TARGET_IMAGE_TYPE"
          else
            echo "Configuration unchanged"
          fi

          echo "BUILD_VERSION=$TARGET_VERSION" >> $GITHUB_OUTPUT
          echo "BUILD_IMAGE_TYPE=$TARGET_IMAGE_TYPE" >> $GITHUB_OUTPUT
          echo "CONTAINERFILE=$CONTAINERFILE_PATH" >> $GITHUB_OUTPUT
          echo "ENABLE_PLYMOUTH=$ENABLE_PLYMOUTH" >> $GITHUB_OUTPUT
          echo "::endgroup::"

      - name: Generate image tags and labels
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.IMAGE_BASE }}
            ${{ env.DOCKERHUB_IMAGE_BASE }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,format=long
            type=ref,event=branch
            type=schedule,pattern=nightly
          labels: |
            org.opencontainers.image.licenses=MIT
            org.opencontainers.image.description=Custom Fedora bootc image with Sway desktop
            org.opencontainers.image.documentation=https://docs.fedoraproject.org/en-US/bootc/

      - name: Extract first tag for testing
        id: first-tag
        run: |
          FIRST_TAG=$(echo "${{ steps.meta.outputs.tags }}" | head -n1)
          echo "tag=$FIRST_TAG" >> $GITHUB_OUTPUT
          echo "First tag: $FIRST_TAG"

      - name: Create auth.json secret for build
        run: |
          mkdir -p ./bootc-secrets
          GHCR_AUTH=$(printf "%s:%s" "${{ github.actor }}" "${{ secrets.GHCR_PAT }}" | base64 -w0)
          DOCKERHUB_AUTH=$(printf "%s:%s" "${{ secrets.DOCKERHUB_USERNAME }}" "${{ secrets.DOCKERHUB_TOKEN }}" | base64 -w0)

          cat > ./bootc-secrets/auth.json <<EOF
          {
            "auths": {
              "ghcr.io": {"auth": "${GHCR_AUTH}"},
              "docker.io": {"auth": "${DOCKERHUB_AUTH}"},
              "docker.io/1borninthedark/exousia": {"auth": "${DOCKERHUB_AUTH}"}
            }
          }
          EOF

      - name: Build image with Buildah
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: exousia
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          containerfiles: ${{ steps.config.outputs.CONTAINERFILE }}
          build-args: |
            FEDORA_VERSION=${{ steps.config.outputs.BUILD_VERSION }}
            IMAGE_TYPE=${{ steps.config.outputs.BUILD_IMAGE_TYPE }}
            ENABLE_PLYMOUTH=${{ steps.config.outputs.ENABLE_PLYMOUTH }}
          oci: true
          tls-verify: true
          layers: true

      - name: Check image size
        run: |
          echo "::group::Image Size Check"
          FIRST_TAG="${{ steps.first-tag.outputs.tag }}"
          SIZE=$(buildah inspect "$FIRST_TAG" | jq -r '.Size')
          SIZE_MB=$((SIZE / 1024 / 1024))
          echo "Image size: ${SIZE_MB}MB"

          if [ $SIZE_MB -gt 4096 ]; then
            echo "::warning::Image size (${SIZE_MB}MB) exceeds 4GB. Consider optimizing."
          fi

          echo "IMAGE_SIZE_MB=${SIZE_MB}" >> $GITHUB_ENV
          echo "::endgroup::"

      - name: Save image as artifact
        run: |
          echo "::group::Saving Image Artifact"
          FIRST_TAG="${{ steps.first-tag.outputs.tag }}"
          echo "Saving image: $FIRST_TAG"
          buildah push "$FIRST_TAG" "oci-archive:exousia-image.tar:$FIRST_TAG"
          echo "Image saved to exousia-image.tar"
          ls -lh exousia-image.tar
          echo "::endgroup::"

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: exousia-container-image
          path: exousia-image.tar
          retention-days: 1
          compression-level: 0

      - name: Build summary
        if: always()
        run: |
          {
            echo "## 🏗️ Build Configuration"
            echo ""
            echo "- **Fedora Version:** ${{ steps.config.outputs.BUILD_VERSION }}"
            echo "- **Base Image Type:** ${{ steps.config.outputs.BUILD_IMAGE_TYPE }}"
            echo "- **Containerfile:** \`${{ steps.config.outputs.CONTAINERFILE }}\`"
            echo "- **Plymouth:** ${{ steps.config.outputs.ENABLE_PLYMOUTH }}"
            echo "- **Image Size:** ${IMAGE_SIZE_MB:-Unknown}MB"
            echo ""
            echo "### 🏷️ Image Tags"
            echo '```'
            echo "${{ steps.meta.outputs.tags }}"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  # ══════════════════════════════════════════════════════════════════════════
  # Job 3: Test - Integration & Compliance Tests
  # ══════════════════════════════════════════════════════════════════════════
  test:
    name: Test Container Image
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: exousia-container-image

      - name: Load image from artifact
        run: |
          echo "::group::Loading Image"
          echo "Loading image from exousia-image.tar"
          ls -lh exousia-image.tar
          buildah pull "oci-archive:exousia-image.tar"
          echo "Image loaded successfully"
          buildah images
          echo "::endgroup::"

      - name: Setup Bats testing framework
        id: setup-bats
        uses: bats-core/bats-action@3.0.0

      - name: Run Bats integration tests
        shell: bash
        env:
          BATS_LIB_PATH: ${{ steps.setup-bats.outputs.lib-path }}
          TERM: xterm
          ENABLE_PLYMOUTH: ${{ needs.build.outputs.build_version }}
        run: |
          echo "::group::Integration Tests"
          FIRST_TAG="${{ needs.build.outputs.first_tag }}"
          echo "Running tests against image: $FIRST_TAG"

          if TEST_IMAGE_TAG="$FIRST_TAG" \
             buildah unshare -- bats -r custom-tests --formatter tap | tee test-results.tap; then
            echo "::notice::Integration tests passed"
            PASSED=$(grep -c "^ok" test-results.tap || echo "0")
            echo "TEST_PASSED=$PASSED" >> $GITHUB_ENV
          else
            echo "::error::Integration tests failed"
            echo "TEST_PASSED=0" >> $GITHUB_ENV
            grep "^not ok" test-results.tap || true
            exit 1
          fi
          echo "::endgroup::"

      - name: Verify bootc container lint
        run: |
          echo "::group::Bootc Container Lint"
          FIRST_TAG="${{ needs.build.outputs.first_tag }}"
          echo "Verifying bootc container compliance for: $FIRST_TAG"
          ctr=$(buildah from "$FIRST_TAG")

          if buildah run "$ctr" bootc container lint > lint-output.txt 2>&1; then
            echo "::notice::bootc container lint passed"
            echo "BOOTC_LINT=passed" >> $GITHUB_ENV
          else
            echo "::error::bootc container lint failed"
            cat lint-output.txt
            buildah rm "$ctr"
            echo "BOOTC_LINT=failed" >> $GITHUB_ENV
            exit 1
          fi

          buildah rm "$ctr"
          echo "::endgroup::"

      - name: Test summary
        if: always()
        run: |
          {
            echo "## 🧪 Test Results"
            echo ""
            echo "- **Integration Tests:** ${TEST_PASSED:-0} tests passed"
            echo "- **Bootc Lint:** ${BOOTC_LINT:-unknown}"
            echo "- **Test Framework:** Bats"
            echo "- **Test Directory:** custom-tests/"
          } >> "$GITHUB_STEP_SUMMARY"

  # ══════════════════════════════════════════════════════════════════════════
  # Job 4: Scan - Security Vulnerability Scanning
  # ══════════════════════════════════════════════════════════════════════════
  scan:
    name: Security Scan
    needs: [build, test]
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: exousia-container-image

      - name: Load image from artifact
        run: |
          echo "::group::Loading Image"
          echo "Loading image from exousia-image.tar"
          buildah pull "oci-archive:exousia-image.tar"
          echo "Image loaded successfully"
          buildah images
          echo "::endgroup::"

      - name: Cache Trivy database
        uses: actions/cache@v4
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ runner.os }}-v2

      - name: Install Trivy
        run: |
          curl -sSfL https://github.com/aquasecurity/trivy/releases/download/v0.52.2/trivy_0.52.2_Linux-64bit.tar.gz \
            | tar -xz -C /usr/local/bin trivy
          trivy --version

      - name: Trivy vulnerability scan
        continue-on-error: true
        run: |
          echo "::group::Trivy Vulnerability Scan"
          FIRST_TAG="${{ needs.build.outputs.first_tag }}"

          if trivy image "$FIRST_TAG" \
            --scanners vuln \
            --severity CRITICAL,HIGH \
            --exit-code 1 \
            --format table; then
            echo "TRIVY_STATUS=passed" >> $GITHUB_ENV
          else
            echo "::warning::Trivy found CRITICAL or HIGH vulnerabilities."
            echo "TRIVY_STATUS=found_issues" >> $GITHUB_ENV
          fi
          echo "::endgroup::"

      - name: Generate Trivy SARIF report
        continue-on-error: true
        run: |
          FIRST_TAG="${{ needs.build.outputs.first_tag }}"
          trivy image "$FIRST_TAG" \
            --scanners vuln \
            --format sarif \
            --output trivy-results.sarif

      - name: Upload Trivy results to GitHub Security
        continue-on-error: true
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif

      - name: Scan summary
        if: always()
        run: |
          {
            echo "## 🔍 Security Scan Results"
            echo ""
            echo "- **Trivy Scan:** ${TRIVY_STATUS:-unknown}"
            echo "- **Scan Type:** Vulnerability (CRITICAL, HIGH)"
            echo "- **SARIF Report:** Uploaded to GitHub Security"
          } >> "$GITHUB_STEP_SUMMARY"

  # ══════════════════════════════════════════════════════════════════════════
  # Job 5: Push & Sign - Publish and Sign Container Images
  # ══════════════════════════════════════════════════════════════════════════
  push-and-sign:
    name: Push & Sign
    needs: [build, test, scan]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: github.event_name != 'pull_request'
    outputs:
      ghcr_digest: ${{ steps.push-images.outputs.ghcr_digest }}
      dockerhub_digest: ${{ steps.push-images.outputs.dockerhub_digest }}

    steps:
      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: exousia-container-image

      - name: Load image from artifact
        run: |
          echo "::group::Loading Image"
          echo "Loading image from exousia-image.tar"
          buildah pull "oci-archive:exousia-image.tar"
          echo "Image loaded successfully"
          buildah images
          echo "::endgroup::"

      - name: Login to GitHub Container Registry
        run: |
          echo "${{ secrets.GHCR_PAT }}" | buildah login -u "${{ github.actor }}" --password-stdin ghcr.io

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | buildah login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin docker.io

      - name: Tag and push images
        id: push-images
        run: |
          echo "::group::Tagging and Pushing Images"
          FIRST_TAG="${{ needs.build.outputs.first_tag }}"

          # Parse all tags and push to registries
          echo "Processing tags from metadata-action..."
          while IFS= read -r full_tag; do
            if [[ -n "$full_tag" ]]; then
              echo "Tagging and pushing: $full_tag"
              buildah tag "$FIRST_TAG" "$full_tag"
              DIGEST=$(buildah push --digestfile /tmp/digest.txt "$full_tag" && cat /tmp/digest.txt)
              echo "Pushed with digest: $DIGEST"

              # Capture digest for the latest tag of each registry
              if [[ "$full_tag" == "${{ env.IMAGE_BASE }}:latest" ]]; then
                echo "$DIGEST" > /tmp/ghcr-digest.txt
              elif [[ "$full_tag" == "${{ env.DOCKERHUB_IMAGE_BASE }}:latest" ]]; then
                echo "$DIGEST" > /tmp/dockerhub-digest.txt
              fi
            fi
          done <<< "${{ needs.build.outputs.image_tags }}"

          # Output digests
          if [[ -f /tmp/ghcr-digest.txt ]]; then
            echo "ghcr_digest=$(cat /tmp/ghcr-digest.txt)" >> $GITHUB_OUTPUT
          fi
          if [[ -f /tmp/dockerhub-digest.txt ]]; then
            echo "dockerhub_digest=$(cat /tmp/dockerhub-digest.txt)" >> $GITHUB_OUTPUT
          fi

          echo "::endgroup::"

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3.5.0

      - name: Login to Docker Hub for signing
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Sign container images (keyless OIDC)
        if: ${{ steps.push-images.outputs.ghcr_digest != '' || steps.push-images.outputs.dockerhub_digest != '' }}
        run: |
          echo "::group::Image Signing"

          if [[ -n "${{ steps.push-images.outputs.ghcr_digest }}" ]]; then
            echo "Signing GHCR image by digest: ${{ steps.push-images.outputs.ghcr_digest }}"
            cosign sign --yes "${{ env.IMAGE_BASE }}@${{ steps.push-images.outputs.ghcr_digest }}"
          else
            echo "No GHCR digest available; skipping GHCR signing"
          fi

          if [[ -n "${{ steps.push-images.outputs.dockerhub_digest }}" ]]; then
            echo "Signing Docker Hub image by digest: ${{ steps.push-images.outputs.dockerhub_digest }}"
            cosign sign --yes "${{ env.DOCKERHUB_IMAGE_BASE }}@${{ steps.push-images.outputs.dockerhub_digest }}"
          else
            echo "No Docker Hub digest available; skipping Docker Hub signing"
          fi

          echo "::endgroup::"
        env:
          COSIGN_EXPERIMENTAL: "true"

      - name: Push summary
        if: always()
        run: |
          {
            echo "## 📦 Published Images"
            echo ""
            echo "### GitHub Container Registry"
            echo "- **Digest:** \`${{ steps.push-images.outputs.ghcr_digest }}\`"
            echo "- **Pull Command:**"
            echo '```bash'
            echo "sudo bootc switch ${{ env.IMAGE_BASE }}:latest"
            echo '```'
            echo ""
            echo "### Docker Hub"
            echo "- **Digest:** \`${{ steps.push-images.outputs.dockerhub_digest }}\`"
            echo "- **Pull Command:**"
            echo '```bash'
            echo "sudo bootc switch ${{ env.DOCKERHUB_IMAGE_BASE }}:latest"
            echo '```'
            echo ""
            echo "### 🔐 Signatures"
            echo "- **Signing Method:** Cosign (Sigstore, keyless OIDC)"
            echo "- **Verification:** \`cosign verify\` with OIDC issuer"
          } >> "$GITHUB_STEP_SUMMARY"

  # ══════════════════════════════════════════════════════════════════════════
  # Job 6: Report - Final Summary and Documentation
  # ══════════════════════════════════════════════════════════════════════════
  report:
    name: Generate Report
    needs: [build, test, scan, push-and-sign]
    runs-on: ubuntu-latest
    if: always() && github.event_name != 'pull_request' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate dynamic README
        run: |
          chmod +x ./custom-scripts/generate-readme
          ./custom-scripts/generate-readme

      - name: Commit updated README and version file
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          if [[ -n $(git status --porcelain README.md .fedora-version) ]]; then
            git add README.md .fedora-version
            git commit -m "docs: auto-update README and version file [skip ci]"
            git push
          else
            echo "No changes to commit"
          fi

      - name: Final summary
        if: always()
        run: |
          {
            echo "## 🎉 Pipeline Complete"
            echo ""
            echo "### Build Information"
            echo "- **Version:** ${{ needs.build.outputs.build_version }}"
            echo "- **Type:** ${{ needs.build.outputs.build_type }}"
            echo "- **Image:** \`${{ needs.build.outputs.first_tag }}\`"
            echo ""
            echo "### Pipeline Status"
            echo "- Build: ${{ needs.build.result }}"
            echo "- Test: ${{ needs.test.result }}"
            echo "- Scan: ${{ needs.scan.result }}"
            echo "- Push & Sign: ${{ needs.push-and-sign.result }}"
            echo ""
            echo "### Resources"
            echo "- [Fedora bootc Documentation](https://docs.fedoraproject.org/en-US/bootc/)"
            echo "- [bootc Project](https://bootc-dev.github.io/bootc/)"
            echo "- [Repository](https://github.com/${{ github.repository }})"
          } >> "$GITHUB_STEP_SUMMARY"
