# Packages to Remove
# These packages are removed from the base image to avoid conflicts or bloat

metadata:
  name: remove
  type: common
  description: Packages to remove from the base image

# Packages to remove
packages:
  - firefox-langpacks
  - firefox
  - sddm
  - foot  # Using kitty/alacritty instead
  - dunst  # Using mako/swaync instead
  - swaylock  # Using swaylock-effects from COPR instead
  - sway-config-upstream  # Remove upstream config, use sway-config-minimal for headless/container use
  - rofi-wayland  # Conflicts with rofi


